using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ddadda : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }
    // da do the do the doooooooooo lo l

    // Update is called once per frame
    void Update()
    {
        
    }
}
